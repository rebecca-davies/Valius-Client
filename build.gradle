/*
 * This build file was generated by the Gradle 'init' task.
 *
 * This generated file contains a commented-out sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.3/userguide/tutorial_java_projects.html
 */
buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
        maven { url "https://oss.sonatype.org/content/repositories/snapshots" }
    }
    dependencies {
        // provides javafx-gradle-plugin
       // classpath group: 'de.dynamicfiles.projects.gradle.plugins', name: 'javafx-gradle-plugin', version: "8.8.2"
        // provides proguard.gradle.ProGuardTask (including all requirements)
        classpath 'net.sf.proguard:proguard-gradle:6.0.2'
    }
}

// Apply the java plugin to add support for Java
apply plugin: 'java'
sourceCompatibility = 1.8
targetCompatibility = 1.8

// In this section you declare where to find the dependencies of your project
repositories {
    // Use 'jcenter' for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
    mavenCentral()
	maven { url 'http://repo.runelite.net' }
}


sourceSets {
    main {
        java {
            srcDir 'src/main/java'
        }
        resources {
        	srcDir 'src/main/resources/'
        }
    }
}


dependencies {

    compile group: 'org.slf4j', name:'slf4j-api', version: '1.7.25'
    
	compile group: 'com.google.guava', name: 'guava', version: '27.0.1-jre'
    compile group: 'ch.qos.logback', name: 'logback-classic', version: '1.2.3'
    
    compile group: 'org.apache.commons', name: 'commons-lang3', version: '3.8.1'
    compile group: 'commons-io', name: 'commons-io', version: '2.6'
    
    compile group: 'com.google.code.gson', name: 'gson', version: '2.8.5'
    compile group: 'net.runelite.pushingpixels', name: 'trident', version: '1.5.00'
    compile group: 'net.runelite.pushingpixels', name: 'substance', version: '8.0.02'
    // https://mvnrepository.com/artifact/org.projectlombok/lombok
    // https://mvnrepository.com/artifact/com.google.inject/guice
	compile group: 'com.google.inject', name: 'guice', version: '4.2.2'
	compile group: 'com.jcabi', name: 'jcabi-manifests', version: '1.1'
	compile 'club.minnced:java-discord-rpc:2.0.2'
    
	compileOnly group: 'org.projectlombok', name: 'lombok', version: '1.18.8'
	annotationProcessor 'org.projectlombok:lombok:1.18.8'
    testCompile 'junit:junit:4.12'
    
}

jar {
	archiveName = "client.jar"
	manifest {
		attributes(
			'Main-Class': 'com.client.Client',
			'Implementation-Version': '2.0.2',
			'Target-Platforms': 'win32-x86-64, win32-x86, linux-x86-64, darwin',
			'Mode': 'LIVE'
			
		)
	}
    from configurations.compile.collect { it.isDirectory() ? it : zipTree(it).matching{ exclude{it.name.contains('MANIFEST.MF') } } }

}

task betaJar(type: Jar) {
	from sourceSets.main.output
	archiveName = "client.jar"
		manifest {
			attributes(
				'Main-Class': 'com.client.Client',
				'Implementation-Version': '2.0.2',
				'Target-Platforms': 'win32-x86-64, win32-x86, linux-x86-64, darwin',
				'Mode': 'BETA'
			)
		}
	    from configurations.compile.collect { it.isDirectory() ? it : zipTree(it).matching{ exclude{it.name.contains('MANIFEST.MF') } } }

}

task cleanBetaClient(type: Delete){
	delete 'C:/xampp/htdocs/beta-client.jar'
}
task copyBetaClient(type: Copy){
	 from 'build/libs/deploy/'
	 into 'C:/xampp/htdocs/'
	 rename 'client.jar', 'beta-client.jar'
}

task buildBetaClient()


task myProguardTask(type: proguard.gradle.ProGuardTask) {
    configuration 'proguard.conf'
    
    libraryjars "${System.getProperty('java.home')}/lib/rt.jar"
    libraryjars "${System.getProperty('java.home')}/lib/ext/jfxrt.jar"
    configurations.runtime.resolve().each {
        libraryjars file(it.getAbsolutePath())
    }

    injars 'build/libs/client.jar'
    outjars 'build/libs/deploy/client.jar'
}


jar.finalizedBy(myProguardTask)


betaJar.dependsOn(compileJava)
betaJar.finalizedBy(myProguardTask)
copyBetaClient.group = ''
cleanBetaClient.group = ''
betaJar.group = ''
buildBetaClient.group = 'beta'
copyBetaClient.dependsOn(betaJar, cleanBetaClient)
buildBetaClient.dependsOn(copyBetaClient)


